```bash
   kubctl create secret generic jwt-secret --from-literal=JWT_KEY=asdf
   kubectl get services -n ingress-nginx
   kubectl get namespace
   kubectl port-forward <pod-name> <local-port>:<pod-port>
```
